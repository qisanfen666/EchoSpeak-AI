# Python AI Engine — Dependencies

# ---- Web framework ----
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.12

# ---- gRPC (production communication with Go gateway) ----
grpcio>=1.66.0
grpcio-tools>=1.66.0
protobuf>=5.28.0

# ---- ASR — 本地语音识别 (集成引擎: faster-whisper) ----
faster-whisper>=1.1.1

# ---- ASR — 流式语音识别 (FunASR 备选) ----
# funasr>=1.1.0

# ---- TTS — 语音合成 (集成引擎: edge-tts) ----
edge-tts>=6.1.0

# ---- LLM — 对话生成 ----
openai>=1.50.0

# ---- 音频处理 ----
soundfile>=0.12.0
numpy>=1.26.0

# ---- 工具 ----
redis>=5.1.0
python-dotenv>=1.0.0
pydantic>=2.0.0
websockets>=12.0
