version: "3.9"

# 仅用于本地开发 — Redis + 可选的 gRPC 测试
services:
  redis:
    image: redis:7-alpine
    container_name: echospeak-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes

volumes:
  redis_data:
